# Copy to `.credentials` for local dev (gitignored). In prod these are GitHub Actions
# secrets, never committed.
SUPERUSER_EMAIL=admin@englishmania.local
SUPERUSER_PASS=change-me
EDITOR_PASS=change-me
